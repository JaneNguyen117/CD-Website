---
import BaseLayout from '../layouts/BaseLayout.astro';
import PageIntro from '../components/PageIntro.astro';
import ServicePanel from '../components/ServicePanel.astro';
import ContactOptions from '../components/ContactOptions.astro';
import { services } from '../content/site';
import { serviceSchema, webPageSchema } from '../utils/schema';

const description =
  'Voice telecom services from CommsDock: PBX engineering, contact centre architecture, voice operations software, and voice AI systems.';
---

<BaseLayout
  title="Services"
  description={description}
  schema={[webPageSchema('/services', 'CommsDock Services', description), ...services.map(serviceSchema)]}
>
  <PageIntro
    label="Services"
    title="Voice telecom first. AI where the call flow needs it."
    summary="CommsDock helps teams design, modernise, and operate PBX, contact centre, and collaboration systems, then builds the integrations and tools that remove voice operations friction."
  />
  <section class="container services-list">
    {services.map((service) => <ServicePanel {...service} />)}
  </section>
  <ContactOptions />
</BaseLayout>

<style>
  .services-list {
    padding-bottom: 32px;
  }
</style>
