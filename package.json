{
  "name": "commsdock",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Solo voice telecom engineering practice for PBX, contact centre, collaboration platforms, and voice AI systems.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "test": "npm run build && node --test tests/*.test.mjs",
    "test:ai": "npm run build && node --test tests/ai-discoverability.test.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/mdx": "^5.0.6",
    "@astrojs/rss": "^4.0.12",
    "@astrojs/sitemap": "^3.3.0",
    "astro": "^6.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.2",
    "typescript": "^5.6.3"
  }
}
